# Tailored Resumes

Generated by `/tailor-resume`. One subfolder per application:

```
tailored-resumes/
  acme__senior-product-manager__2026-04-22/
    resume.md          # ATS-optimized Markdown (canonical)
    resume.docx        # Optional .docx export (preferred for ATS upload)
    keywords-matched.md # Match report against the JD
    ats-audit.md       # Output of /ats-audit
```

The Markdown is the source of truth — render it to .docx with Pandoc when you need to upload.
